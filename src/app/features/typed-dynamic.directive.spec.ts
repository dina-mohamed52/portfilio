import { TypedDynamicDirective } from './typed-dynamic.directive';

describe('TypedDynamicDirective', () => {
  it('should create an instance', () => {
    const directive = new TypedDynamicDirective();
    expect(directive).toBeTruthy();
  });
});
